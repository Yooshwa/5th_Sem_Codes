# Design a program that accepts user input as a string and converts it into different data
# types (int, float) using type conversion functions.

n = int(input("Enter an integer: "))

m=float(n)


print(f"{n}",type(n))
print(f"{m}",type(m))